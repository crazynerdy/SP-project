# SP 2026 智能体自动填写 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 SP 战略规划智能体新增"驱动文件驱动的 2026 自动填写流程"。读 `D:\Desktop\gg\战略规划SP变更需求-6.5 版（产品需求沟通确认 2.0）.xlsx` 找"智能体自动..."行，试点 2.2 行业趋势分析表：从 iDSTE MCP 拉 2025 数据作 few-shot → LLM + DuckDuckGo 生成 2026 内容 → 覆盖写主模板 B4-B9 → 输出到 `D:\memory\output\`，Tab 1 加表单入口。

**Architecture:** 复用现有 `models/idste.py` / `models/websearch.py` / `models/llm.py` / `models/xlsx_exporter.py` 全部基础设施。新增 1 个 model (`models/sp_change_2026.py`)、1 个 controller (`controllers/sp_change_2026_controller.py`)、1 个 view (`views/sp_change_2026_view.py`)；在 `models/xlsx_exporter.py` 加 `fill_cells()` 方法（不动原 `fill_template`）、`models/prompts.py` 加 `build_sp_2026_data_prompt()`、`views/excel_view.py` 加表单子区块、`app.py` 拼接新 view。**单点可工作**，扩到 22 张时**不开新架构**：驱动文件解析已识别全部 22 行，prompt 6 维度从主模板现读，`fill_cells` 是 dict-based。

**Tech Stack:** Python 3.10+, openpyxl, requests, streamlit, duckduckgo-search, 复用 Qwen3.5_122B_A10B (OpenAI 兼容) LLM 客户端、iDSTE MCP REST 客户端。

**Spec 引用:** `docs/superpowers/specs/2026-07-15-sp-2026-auto-fill-design.md`

---

## 执行范围（2026-07-15 用户确认）

**已确认可做**:
- Task 1: `models/sp_change_2026.py` — parse_requirement_xlsx + resolve_table_key（unit-testable）
- Task 2: `models/xlsx_exporter.py` 加 `fill_cells()` 方法
- Task 3: `models/prompts.py` 加 `build_sp_2026_data_prompt()`
- Task 4: `controllers/sp_change_2026_controller.py` — run_sp_change_2026（端到端集成）
- Task 5: `views/sp_change_2026_view.py` + `views/excel_view.py` 加子区块 + `app.py` 拼接
- Task 6: L3 端到端验证（需 VPN + LLM + 主模板）+ STATUS.md 更新

**⏸ 不在本次范围**:
- 扩到全 22 张「智能体自动...」sheet（架构已留扩展点，单独 spec）
- 多年型 sheet (6.1/6.2) Y1/Y2/Y3 重映射（单独 spec）
- `.env` 加 `SP_2026_TABLE_KEY_MAP` 显式映射（本次单一关键字匹配）
- 驱动文件 gg Excel 自动检测（本次 UI 上传/输入路径）

**为什么这样切**: 用户原话"做吧，重点还是易扩展可迭代"。先让 2.2 跑通，未来扩 22 张时**不引入新的复杂度层级**（沿用现有抽象）。

---

## Global Constraints

- **项目根目录:** `D:\memory`
- **Python 解释器:** 系统中已有的 `python`（已装 openpyxl, requests, streamlit, python-dotenv, duckduckgo-search）
- **LLM:** Qwen3.5_122B_A10B at `http://10.8.0.54:6003/v1`（OpenAI 兼容，function-calling）
- **iDSTE MCP:** `http://123.188.239.53:9090`，5 个只读工具（`sp_team_info / sp_plan / sp_dimension / sp_data_menu / sp_data`）
- **主模板:** `D:\下载\【中海润】战略规划智能体输出格式.xlsx`（37 张 sheet 的 DSTE/BLM 标准模板）
- **驱动文件:** `D:\Desktop\gg\战略规划SP变更需求-6.5 版（产品需求沟通确认 2.0）.xlsx`
- **输出目录:** `D:\memory\output\`
- **代码风格:** 易扩展可迭代——DI、配置化、schema 驱动、单一真相源；中文输出；每文件 < 300 行
- **测试方式:** 每个新模块带 `__main__` 段做轻量验证；不写正式 pytest
- **凭据:** 全部在 `.env`（已 gitignore）
- **Git:** 项目已 git 化；每任务一步一 commit；commit 走英文约定式
- **不破坏现有流程:** `fill_template` / `generate_xlsx_data` / `run_excel_flow` / `app.py` Tab 1 现有逻辑全部不动

---

## 文件结构总览

| 文件 | 操作 | 职责 |
|---|---|---|
| `models/sp_change_2026.py` | 新 | 业务核心：parse_requirement_xlsx / resolve_table_key |
| `controllers/sp_change_2026_controller.py` | 新 | 编排：run_sp_change_2026 |
| `views/sp_change_2026_view.py` | 新 | Streamlit 子区块 UI（表单 + 进度 + 下载） |
| `models/xlsx_exporter.py` | 改 | 加 `fill_cells()` 方法（旁加，不动 `fill_template`） |
| `models/prompts.py` | 改 | 加 `build_sp_2026_data_prompt()` 动态拼装 |
| `views/excel_view.py` | 改 | Tab 1 末尾加子区块 |
| `app.py` | 改 | 把 sp_change_2026_view 接入 Tab 1（仅 import 拼接） |
| `STATUS.md` | 改 | v5 增量章节 |

---

## Task 1: `models/sp_change_2026.py` 解析 + MCP table_key 解析

**Files:**
- Create: `D:\memory\models\sp_change_2026.py`
- Test: `D:\memory\models\sp_change_2026.py` 的 `__main__` 段

**Interfaces:**
- Consumes: 驱动文件路径；`list[dict]` (sp_data_menu 返回的 menu 列表)
- Produces:
  - `parse_requirement_xlsx(path: str) -> list[RequirementRow]`
  - `resolve_table_key(template_name: str, menu: list[dict], keyword: str) -> str`
  - `RequirementRow` dataclass：8 字段（A-H 列）

**核心原则**：解析 + 解析都是纯函数，无副作用，**不带 LLM 调用**。这样可以**离线单元测试**。

- [ ] **Step 1: 写 `models/sp_change_2026.py`（含 dataclass + 2 个解析函数）**

```python
# -*- coding: utf-8 -*-
"""SP 2026 智能体自动填写 - 业务核心。

解析驱动文件 (gg/战略规划SP变更需求-*.xlsx) -> 过滤"智能体自动..."行 ->
对每行调 MCP 拉 2025 实际数据 (few-shot) -> LLM + 联网生成 2026 单元格数据。
"""
from __future__ import annotations
import os
import re
from dataclasses import dataclass, asdict
from openpyxl import load_workbook
from models import idste


AUTO_FILL_MARKERS = {"智能体自动搜索填写", "智能体自动分析总结"}


@dataclass
class RequirementRow:
    """驱动文件里一行 (BLM 战略规划研究内容追踪表)。"""
    row: int
    blm_module: str
    stage_step: str
    research_content: str
    output_template: str
    data_source: str
    process_hint: str
    idste_module: str
    note: str

    def to_dict(self) -> dict:
        return asdict(self)


def parse_requirement_xlsx(path: str) -> list[RequirementRow]:
    """读驱动文件 Excel, 过滤 E in AUTO_FILL_MARKERS 的行。"""
    if not os.path.exists(path):
        raise FileNotFoundError(f"驱动文件不存在: {path}")
    if not path.lower().endswith((".xlsx", ".xlsm")):
        raise ValueError(f"驱动文件必须是 .xlsx: {path}")

    wb = load_workbook(path, data_only=True)
    ws = wb[wb.sheetnames[0]]

    rows: list[RequirementRow] = []
    for r_idx in range(3, ws.max_row + 1):
        a = ws.cell(row=r_idx, column=1).value
        e = ws.cell(row=r_idx, column=5).value
        if e is None:
            continue
        e_str = str(e).strip()
        if e_str not in AUTO_FILL_MARKERS:
            continue
        rows.append(RequirementRow(
            row=r_idx,
            blm_module=str(a or "").strip(),
            stage_step=str(ws.cell(row=r_idx, column=2).value or "").strip(),
            research_content=str(ws.cell(row=r_idx, column=3).value or "").strip(),
            output_template=str(ws.cell(row=r_idx, column=4).value or "").strip(),
            data_source=e_str,
            process_hint=str(ws.cell(row=r_idx, column=6).value or "").strip(),
            idste_module=str(ws.cell(row=r_idx, column=7).value or "").strip(),
            note=str(ws.cell(row=r_idx, column=8).value or "").strip(),
        ))
    return rows


def _normalize_template_name(name: str) -> str:
    """去"表"字 + 去空白, 方便与 menu 里 table.name 模糊匹配。"""
    return re.sub(r"\s+", "", name).rstrip("表")


def resolve_table_key(template_name: str, menu, keyword: str | None = None) -> str:
    """把 D 列里的"输出表格/图形模版"名解析为 MCP table_key。"""
    candidates = idste._find_table_keys(menu)
    if not candidates:
        raise ValueError(
            f"sp_data_menu 返回里未发现任何 table_key, 无法解析 {template_name!r}"
        )

    normalized = _normalize_template_name(template_name)
    if keyword is None:
        keyword = "".join(re.findall(r"[一-鿿]+", normalized))

    for c in candidates:
        cname = re.sub(r"\s+", "", c.get("name") or "")
        for chunk in re.findall(r"[一-鿿]{2,}", normalized):
            if chunk in cname:
                return c["table_key"]

    for c in candidates:
        cname = re.sub(r"\s+", "", c.get("name") or "")
        if keyword and keyword in cname:
            return c["table_key"]

    raise ValueError(
        f"主模板 sheet {template_name!r} (normalized={normalized!r}, "
        f"keyword={keyword!r}) 未在 MCP 菜单中找到。\n"
        f"MCP 菜单候选: {[c.get('name') for c in candidates[:8]]}"
    )


if __name__ == "__main__":
    import sys

    DRIVE_FILE = r"D:\Desktop\gg\战略规划SP变更需求-6.5 版（产品需求沟通确认 2.0）.xlsx"

    print("=" * 60)
    print("== [1] parse_requirement_xlsx 离线单元测试 ==")
    rows = parse_requirement_xlsx(DRIVE_FILE)
    print(f"驱动文件: {DRIVE_FILE}")
    print(f"发现 {len(rows)} 行「智能体自动...」:")
    for r in rows:
        print(f"  row={r.row} D={r.output_template!r} E={r.data_source!r}")

    print()
    print("=" * 60)
    print("== [2] resolve_table_key 在线测试 (需 VPN/iDSTE) ==")
    if len(sys.argv) > 1 and sys.argv[1] == "--online":
        menu = idste.sp_data_menu("c")
        for r in rows[:3]:
            try:
                tk = resolve_table_key(r.output_template, menu)
                print(f"  {r.output_template} -> {tk} OK")
            except ValueError as e:
                print(f"  {r.output_template} -> FAIL: {e}")
    else:
        print("  (跳过; 跑 `python -m models.sp_change_2026 --online` 启用)")
```

- [ ] **Step 2: 验证文件能 import + 离线单测通过**

```bash
python -c "from models.sp_change_2026 import parse_requirement_xlsx, resolve_table_key, RequirementRow; print('import OK')"
```

预期: 输出 `import OK`。

- [ ] **Step 3: 跑离线单测段**

```bash
python -m models.sp_change_2026
```

预期: 输出 `发现 22 行「智能体自动...」:` (或类似数字), 每行列出 `row=6 D='2.2 行业趋势分析表' E='智能体自动搜索填写'`。**实际行数 = 22**。

如果行数不对, 停下来检查驱动文件读取逻辑 (行号偏移、列索引)。

- [ ] **Step 4:（可选）跑在线 resolve 测试**

仅在 VPN/iDSTE 可达时跑:

```bash
python -m models.sp_change_2026 --online
```

预期: 每行输出 `2.2 行业趋势分析表 -> <table_key> OK`。
如果某行 `FAIL`, 记录错信息, **不阻塞** (这是给后续 Task 4 看的)。

- [ ] **Step 5: Commit**

```bash
git add models/sp_change_2026.py && git commit -m "feat(sp-change-2026): add parse_requirement_xlsx + resolve_table_key

- Pure functions, offline-testable
- 22 auto-fill rows from driving file
- Normalize template name (strip '表' + whitespace) for fuzzy menu match

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 2: `models/xlsx_exporter.fill_cells()` 覆盖写

**Files:**
- Modify: `D:\memory\models\xlsx_exporter.py` (新增 `fill_cells()` 函数 + `__main__` 测试段)

**Interfaces:**
- Consumes: `template_path: str`, `sheet_name: str`, `cells: dict[str, str]`, `mode: str = "overwrite"`, `out_path: str | None = None`
- Produces: `str` (out_path 绝对路径)

**设计原则**: 与 `fill_template` 平行存在, **不动**原函数。

- [ ] **Step 1: 在 `models/xlsx_exporter.py` 末尾追加 `fill_cells()` + 测试段**

在 `if __name__ == "__main__":` 之前插入:

```python
def fill_cells(template_path, sheet_name, cells, mode="overwrite", out_path=None):
    """把指定 cell 覆盖/追加写入主模板副本。

    与 fill_template 的区别:
    - fill_template 是 LLM 输出"全 sheet"结构 (headers + rows), 批量重写 row 3+
    - fill_cells 是 dict-based (B4 -> "..."), 点对点写具体 cell, **不动其他 cell**

    Raises:
        FileNotFoundError: 模板不存在
        KeyError: sheet_name 不在模板里
        ValueError: mode 未知 或 cells 为空
    """
    if mode not in ("overwrite", "append_row"):
        raise ValueError(f"未知 mode: {mode!r}（仅支持 overwrite / append_row）")
    if not cells:
        raise ValueError("cells 为空")

    if not os.path.exists(template_path):
        raise FileNotFoundError(f"模板不存在: {template_path}")

    if out_path is None:
        from datetime import datetime
        from uuid import uuid4
        ts = datetime.now().strftime("%Y%m%d_%H%M%S")
        out_dir = "output"
        os.makedirs(out_dir, exist_ok=True)
        out_path = os.path.join(out_dir, f"{ts}_sp_2026_{uuid4().hex[:8]}_data.xlsx")
    else:
        os.makedirs(os.path.dirname(out_path) or ".", exist_ok=True)

    shutil.copyfile(template_path, out_path)
    wb = load_workbook(out_path)

    available = {name.strip(): name for name in wb.sheetnames}
    sname_norm = sheet_name.strip()
    if sname_norm not in available:
        raise KeyError(
            f"模板里没有 sheet {sheet_name!r}。已有: {list(wb.sheetnames)[:5]}..."
        )
    real_sname = available[sname_norm]
    ws = wb[real_sname]

    for coord, val in cells.items():
        if mode == "overwrite":
            ws[coord] = val
        elif mode == "append_row":
            if len(cells) != 1:
                raise ValueError("append_row 模式仅支持单 key cells")
            col_letter = re.match(r"^([A-Z]+)", coord).group(1)
            target_col_idx = ws[f"{col_letter}1"].column
            row = ws.max_row + 1
            while row > 1 and ws.cell(row=row - 1, column=target_col_idx).value not in (None, ""):
                row -= 1
            while row <= ws.max_row and ws.cell(row=row, column=target_col_idx).value not in (None, ""):
                row += 1
            ws.cell(row=row, column=target_col_idx, value=val)

    try:
        wb.save(out_path)
    except PermissionError as e:
        raise PermissionError(
            f"xlsx 保存失败（文件可能被 Excel 占用）: {out_path} - {e}"
        ) from e

    return out_path
```

并在文件顶部加 `import re` (如果还没有)。

- [ ] **Step 2: 在 `__main__` 段末尾追加 fill_cells 测试**

替换原 `__main__` 段为:

```python
if __name__ == "__main__":
    from core.envutil import env
    tpl = env("TEMPLATE_XLSX_PATH")

    # --- Test 1: 原 fill_template 不动 ---
    test_data = {
        "sheets": [
            {
                "sheet_name": "1.1 业绩差距分析",
                "headers": ["序号", "业绩差距描述", "存在差距的主要原因", "责任人"],
                "rows": [[1, "测试行 1", "测试原因", "测试人"]],
            }
        ]
    }
    out1 = "output/test_fill_template.xlsx"
    result = fill_template(test_data, tpl, out1)
    print(f"[fill_template] filled: {result['filled']} missing: {result['missing']}")

    # --- Test 2: 新 fill_cells overwrite ---
    test_cells = {"B4": "2026 客户偏好预测：消费者更注重健康环保..."}
    out2 = fill_cells(tpl, "2.2 行业趋势分析", test_cells, mode="overwrite")
    print(f"[fill_cells overwrite] out: {out2}")

    # --- Test 3: 读回验证 ---
    wb = load_workbook(out2)
    ws = wb["2.2 行业趋势分析"]
    print(f"[verify] B4 = {ws['B4'].value!r}")

    # --- Test 4: 多 cell overwrite ---
    test_cells_4 = {
        "B4": "2026 维度 1",
        "B5": "2026 维度 2",
        "B9": "2026 维度 6",
    }
    out3 = fill_cells(tpl, "2.2 行业趋势分析", test_cells_4)
    wb3 = load_workbook(out3)
    ws3 = wb3["2.2 行业趋势分析"]
    for coord, expected in test_cells_4.items():
        actual = ws3[coord].value
        ok = "OK" if actual == expected else "FAIL"
        print(f"[verify multi] {coord} = {actual!r}  {ok}")

    # --- Test 5: sheet 不存在应抛 KeyError ---
    try:
        fill_cells(tpl, "9.9 不存在的 sheet", {"A1": "x"})
        print("[error] 应抛 KeyError 但没抛 FAIL")
    except KeyError:
        print(f"[error path] KeyError 正确抛出 OK")
```

- [ ] **Step 3: 跑 `__main__` 段**

```bash
python -m models.xlsx_exporter
```

预期输出:

```
[fill_template] filled: ['1.1 业绩差距分析'] missing: []
[fill_cells overwrite] out: output/<ts>_sp_2026_<uuid>_data.xlsx
[verify] B4 = '2026 客户偏好预测：消费者更注重健康环保...'
[verify multi] B4 = '2026 维度 1'  OK
[verify multi] B5 = '2026 维度 2'  OK
[verify multi] B9 = '2026 维度 6'  OK
[error path] KeyError 正确抛出 OK
```

- [ ] **Step 4: 跑现有 fill_template 回归（确保没破）**

```bash
python -c "
from models import xlsx_exporter
from core.envutil import env
r = xlsx_exporter.fill_template(
    {'sheets': [{'sheet_name': '1.1 业绩差距分析', 'headers': ['序号','描述'], 'rows': [[1,'test']]}]},
    env('TEMPLATE_XLSX_PATH'),
    'output/regression_test.xlsx'
)
print('regression OK:', r['filled_count'])
"
```

预期: `regression OK: 1` (原 `fill_template` 仍能跑)。

- [ ] **Step 5: Commit**

```bash
git add models/xlsx_exporter.py && git commit -m "feat(xlsx-exporter): add fill_cells() for point-precise cell write

- Parallel to fill_template (which rewrites whole sheets)
- mode='overwrite' (default) and 'append_row'
- Returns out_path; auto-generates <ts>_sp_2026_<uuid>_data.xlsx
- Backward compatible: fill_template untouched

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 3: `models/prompts.build_sp_2026_data_prompt()` 动态 prompt

**Files:**
- Modify: `D:\memory\models\prompts.py` (新增 `build_sp_2026_data_prompt()` + 常量 `SP_2026_DATA_PROMPT_TEMPLATE`)

**Interfaces:**
- Consumes: `template_name: str`, `row_labels: list[str]` (A 列维度), `baseline_2025_json: str`, `process_hint: str`, `year: int`
- Produces: `str` (完整 system prompt)

**设计原则**: 常量 + 单 builder 函数, `row_labels` 从主模板现读传入 (不硬编码 6 维度)。

- [ ] **Step 1: 在 `prompts.py` 末尾加 `SP_2026_DATA_PROMPT_TEMPLATE` + `build_sp_2026_data_prompt()`**

```python
# ============================================================
# 4) SP 2026 智能体自动填写 prompt (驱动文件驱动 + few-shot from MCP 2025)
# ============================================================
SP_2026_DATA_PROMPT_TEMPLATE = """你是【中海润】公司战略规划分析专家。任务:基于 2025 实际数据
(few-shot), 分析并预测 {year} 年【{template_name}】的 {n_dims} 个维度的内容。

## 一、目标表格信息
- 主模板 sheet 名: {template_name}
- 主模板 A 列 (要看的内容): {row_labels_json}
- 目标列: {target_col} (结果列)
- 目标年份: {year}

## 二、2025 实际数据 (few-shot, 来自 iDSTE MCP sp_data)
{baseline_2025_json}

## 三、业务期望过程 (来自驱动文件 F 列)
{process_hint}

## 四、可用工具
- web_search: 搜索 {year} 年最新行业事件、政策、技术 (DuckDuckGo)
  工具质量次于 sp_data; 本场景主要用来查 {year} 新增趋势

## 五、输出严格 JSON (无 markdown 包装, 无前后言)
{{
{cell_keys_example}
}}

## 六、硬规则
- 每个 cell 内容 ≥80 字, 包含 1-2 个具体数据/事件引用
- 不臆造; 引用来自 baseline_2025 或 web_search; {year} 预测必须明确标注"预测"
- 结论先行; 中文; 字符串内不用 ASCII 双引号 (用「」或 "")
- {n_dims} 个 cell 风格、深度对齐
- 仅返 JSON, 不要任何解释
"""


def build_sp_2026_data_prompt(
    template_name: str,
    row_labels: list[str],
    baseline_2025_json: str,
    process_hint: str,
    year: int,
    target_col: str = "B",
    start_row: int = 4,
) -> str:
    """动态拼装 SP 2026 prompt。"""
    n_dims = len(row_labels)
    cell_keys_example = ",\n".join(
        f'  "{target_col}{start_row + i}": "..."' for i in range(n_dims)
    )
    row_labels_json = " | ".join(row_labels) if row_labels else "(无维度标签)"
    return SP_2026_DATA_PROMPT_TEMPLATE.format(
        template_name=template_name,
        n_dims=n_dims,
        row_labels_json=row_labels_json,
        target_col=target_col,
        baseline_2025_json=baseline_2025_json,
        process_hint=process_hint or "（无额外业务期望）",
        year=year,
        cell_keys_example=cell_keys_example,
    )
```

- [ ] **Step 2: 在 `__main__` 段追加 prompt builder 测试**

在 `if __name__ == "__main__":` 末尾追加:

```python
    print("=" * 60)
    print("[4] SP_2026_DATA_PROMPT 长度（2.2 试跑 6 维度）:",
          len(build_sp_2026_data_prompt(
              template_name="2.2 行业趋势分析",
              row_labels=["变化中的客户偏好", "行业间的界限模糊",
                          "全球化/新进入市场的全球化竞争者", "突破性的新技术",
                          "政策、法规变化", "新的或变化中的利润模式"],
              baseline_2025_json='{"示例": "2025 实际数据..."}',
              process_hint="要根据差距分析，然后作为输入，进行生成",
              year=2026,
          )),
          "字")
```

- [ ] **Step 3: 跑 `__main__` 段验证**

```bash
python -m models.prompts
```

预期: 3 个原有 prompt 长度 + 第 4 个 SP 2026 prompt 长度输出 (数字 > 500)。检查 `print` 输出里 6 个 cell key (B4, B5, B6, B7, B8, B9) 都出现在 cell_keys_example 里。

- [ ] **Step 4: Commit**

```bash
git add models/prompts.py && git commit -m "feat(prompts): add build_sp_2026_data_prompt() dynamic builder

- row_labels from template (not hardcoded 6 dims)
- cell_keys_example auto-generated for n_dims
- Reuses f-string + .format() pattern of existing builders

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 4: `controllers/sp_change_2026_controller.py` 端到端集成

**Files:**
- Create: `D:\memory\controllers\sp_change_2026_controller.py`

**接口 (DI 全开)**:
```python
def run_sp_change_2026(
    gg_xlsx_path: str,
    template_xlsx_path: str | None = None,    # None 时从 .env 读
    year: int = 2026,
    target_sheet_names: list[str] | None = None,
    output_dir: str = "output",
    llm_client=None, tool_map=None, prompts_module=None,
    sp_change_2026_module=None, xlsx_exporter_module=None,
    verbose: bool = True,
) -> dict:
    """Returns: {intent_meta, output_xlsx_path, tool_call_stats, filled_sheets, errors}"""
```

**核心流程** (参考 Task 1-3 已建立模式 + `controllers/agent.py` 现有 controller 风格):

1. `parse_requirement_xlsx(gg_xlsx_path)` -> `requirements` 列表
2. (loop 外一次) `idste.sp_dimension()` -> `dim`; `idste.sp_data_menu(dim)` -> `menu`
3. for each `req` in `requirements`:
   - `resolve_table_key(req.output_template, menu)` -> `tk`
   - `idste.sp_data(dim, tk, str(year-1))` -> `baseline_2025` (few-shot)
   - `_read_row_labels_from_template(template, mt_sheet, "A", 4)` -> 6 维度
   - `build_sp_2026_data_prompt(...)` -> system_prompt
   - `llm.run_agent_loop(system_prompt, user_msg, {**idste.TOOL_MAP, **websearch.TOOL_MAP}, [web_search schema], max_turns=8, max_tokens=2000, final_max_tokens=12288, enable_thinking=False, final_enable_thinking=True)`
   - `_extract_json_from_text(final_text)` -> `cells` (容错: 缺 cell 填空字符串)
   - in-place 写: `load_workbook(out_xlsx_path)` -> `ws[mt_sheet][coord] = val` -> `wb.save()`
4. return dict

**关键设计**:
- `sp_dimension` / `sp_data_menu` 在 loop 外**只调一次** (避免 N+1 次 MCP)
- `out_xlsx_path` 第一次写入时创建 (复制主模板), 后续 sheet 累加 in-place
- 每个 sheet 失败不中断, 累计到 `errors[]`
- 所有 sheet 失败 -> raise RuntimeError

**`__main__` 段**: `python -m controllers.sp_change_2026_controller "2.2 行业趋势分析表"` 直接跑试跑

- [ ] **Step 1: 写 `controllers/sp_change_2026_controller.py`** (按上面接口 + 流程, 完整代码 ~180 行)

- [ ] **Step 2: 验证 import**

```bash
python -c "from controllers.sp_change_2026_controller import run_sp_change_2026; print('import OK')"
```

预期: `import OK`。

- [ ] **Step 3:（需 VPN）端到端跑 2.2 试跑**

```bash
python -m controllers.sp_change_2026_controller "2.2 行业趋势分析表"
```

预期: 30-60s 内输出 `OK 完成`, `out: output/<ts>_sp_2026_<uuid>_data.xlsx`, `rows: 6`, `errors: 0`。

- [ ] **Step 4: 读回验证**

```bash
python -c "
import openpyxl, glob, os
files = sorted(glob.glob('output/*_sp_2026_*_data.xlsx'), key=os.path.getmtime)
wb = openpyxl.load_workbook(files[-1])
ws = wb['2.2 行业趋势分析']
for r in range(4, 10):
    print(ws.cell(row=r, column=1).value, '|', (ws.cell(row=r, column=2).value or '')[:80])
"
```

预期: 6 行都有非空内容 (≥80 字)。

- [ ] **Step 5: Commit**

```bash
git add controllers/sp_change_2026_controller.py && git commit -m "feat(sp-2026-controller): run_sp_change_2026 end-to-end orchestration

- DI: llm_client / tool_map / prompts_module / sp_change_2026 / xlsx_exporter
- Reuses idste MCP (sp_dimension/menu/data) + websearch + LLM run_agent_loop
- 1 out_xlsx accumulates writes for multiple sheets (extensible to 22)
- 2.2 pilot: row=6, 6 dimensions, B4-B9 overwrite

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---
## Task 5: Streamlit UI (Tab 1 末尾子区块)

**Files:**
- Create: `D:\memoryiews\sp_change_2026_view.py`
- Modify: `D:\memoryiews\excel_view.py` (追加 `render_excel_tab_with_sp2026`)
- Modify: `D:\memorypp.py` (Tab 1 改走新入口)

**接口**:
- `render_sp_change_2026_form(template_path: str)` - 渲染表单 + 结果 + 下载
- `render_excel_tab_with_sp2026(template_path, idste_healthy)` - 包装原 Tab 1 整体

**UI 元素** (in `views/sp_change_2026_view.py`):
- `st.text_input("驱动文件路径", value=DEFAULT_GG_PATH)` - 默认填好桌面路径
- `st.number_input("目标年份", 2024-2030, 2026)` - year
- `st.text_input("限定 sheet 名 (可选, 逗号分隔)", "2.2 行业趋势分析表")`
- `st.button("生成 2026 报告", type="primary")`
- 触发时: `with st.spinner(...): sp2026_ctrl.run_sp_change_2026(...)` -> 写 session_state
- 结果展示: `st.success(...)` + 工具调用统计 + 错误 expander + `st.download_button(...)`

**集成点**:
- `views/excel_view.py`: 在 `render_excel_results` 后追加 `sp_change_2026_view.render_sp_change_2026_form(template_path)`
- `app.py`: 把 `excel_controller.run(...)` 改为 `excel_view.render_excel_tab_with_sp2026(...)`

- [ ] **Step 1: 创建 `views/sp_change_2026_view.py` (完整代码 ~120 行)**

具体内容: form 包含路径输入 + 年份 + sheet 过滤 + 按钮; _run_with_spinner 调 controller 写 session_state; _render_result 显结果 + 错误 + 下载按钮。

- [ ] **Step 2: 在 `views/excel_view.py` 加 `render_excel_tab_with_sp2026`**

在 `render_excel_results` 函数后追加:

```python
def render_excel_tab_with_sp2026(template_path, idste_healthy):
    """Tab 1 整体渲染 (自然语言 + 智能体自动填写两个区块)。"""
    form_data = render_excel_form(idste_healthy)
    if form_data["clicked"]:
        from controllers import excel_controller as ec
        ec.run_excel(form_data["request"], template_path)
    render_excel_results(st.session_state.get("last_tool_call_stats"), st.session_state.get("last_xlsx_path"))
    from views import sp_change_2026_view
    sp_change_2026_view.render_sp_change_2026_form(template_path)
```

- [ ] **Step 3: 改 `app.py` 让 Tab 1 调用新入口**

替换 `app.py` 中 `if active_tab == "excel":` 块:

```python
if active_tab == "excel":
    from views import excel_view
    excel_view.render_excel_tab_with_sp2026(TEMPLATE_PATH, st.session_state["idste_healthy"])
```

- [ ] **Step 4: 验证 import 不破**

```bash
python -c "from views import excel_view, sp_change_2026_view; from app import TEMPLATE_PATH; print('import OK; template:', TEMPLATE_PATH)"
```

预期: `import OK; template: D:\下载\【中海润】战略规划智能体输出格式.xlsx`

- [ ] **Step 5: 启动 Streamlit 验证 UI 不崩**

```bash
streamlit run app.py --server.headless true --server.port 8502 &
sleep 5
curl -s -o /dev/null -w "%{http_code}" http://localhost:8502
```

预期: HTTP 200。如果 streamlit 报 SyntaxError/ModuleNotFoundError, **回滚**到 `excel_controller.run(TEMPLATE_PATH, ...)` 调用方式排查。

- [ ] **Step 6:（人工）浏览器打开 http://localhost:8502 验证**

- Tab 1 看到 "Excel" 区块 (自然语言表单) - 现有 UI 不动 OK
- Tab 1 末尾看到 "智能体自动填写" 新区块 OK
- 点 "生成 2026 报告" -> 等 30-60s -> 看到下载按钮 OK

- [ ] **Step 7: Commit**

```bash
git add views/sp_change_2026_view.py views/excel_view.py app.py && git commit -m "feat(sp-2026-view): add Tab 1 智能体自动填写 sub-section

- views/sp_change_2026_view.py: form + result + download
- views/excel_view.py: render_excel_tab_with_sp2026 wraps both sections
- app.py: Tab 1 routes to new entry point (backward compatible)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---
## Task 6: 端到端验证 + STATUS.md

**Files:**
- Modify: `D:\memory\STATUS.md` (新增 v5 增量章节)

- [ ] **Step 1: 跑完整 2.2 端到端**

```bash
python -m controllers.sp_change_2026_controller "2.2 行业趋势分析表"
```

预期: 30-60s 后输出 `OK 完成`, `rows: 6`, `errors: 0`。

- [ ] **Step 2: 人工审核 6 个 cell 内容质量**

```bash
python -c "
import openpyxl, glob, os
files = sorted(glob.glob('output/*_sp_2026_*_data.xlsx'), key=os.path.getmtime)
wb = openpyxl.load_workbook(files[-1])
ws = wb['2.2 行业趋势分析']
for r in range(4, 10):
    b = ws.cell(row=r, column=2).value or ''
    print(f'B{r} ({len(b)}字): {b[:200]}...')
"
```

人工逐条检查 (按 spec § 6 prompt 硬规则):
- 6 个 cell 都 ≥ 80 字
- 含具体数据/事件引用 ("2026 预测" 字样或具体数字)
- 结论先行
- 风格/深度对齐
- 中文引号「」或 ""

**如果质量不达标**: 调 `prompts.py` 的硬规则 (如要求"必须引用 1 个 web_search 结果"), 重跑。

- [ ] **Step 3: 跑现有流程回归 (确保没破)**

```bash
python -c "
from controllers.agent import run_excel_flow
intent, path = run_excel_flow('分析 2025 年公司 SP 战略规划完成度', verbose=False)
print('REGRESSION OK:', path)
"
```

预期: 现有 Excel 流程仍能跑通。

- [ ] **Step 4: 更新 `STATUS.md` 加 v5 增量章节**

在文件末尾追加 v5 增量章节 (新增能力 / 新增文件 / 改动文件 / 验证状态 / 未来扩展点 5 个小节, 模板已在 spec § 11 写好)。

- [ ] **Step 5: Commit**

```bash
git add STATUS.md && git commit -m "docs(status): add v5 增量 for SP 2026 自动填写

Co-Authored-By: Claude <noreply@anthropic.com>"
```

- [ ] **Step 6: 全部任务完成, 做一次总检查**

```bash
git log --oneline -10
```

确认 6 个 commit 都到位 (每任务一步一 commit)。

---

## 验收清单 (Definition of Done)

- [ ] `python -m models.sp_change_2026` 输出 22 行"智能体自动..."
- [ ] `python -m models.xlsx_exporter` 5 个测试用例都 OK
- [ ] `python -m models.prompts` 4 段 prompt 都打印长度
- [ ] `python -m controllers.sp_change_2026_controller "2.2 行业趋势分析表"` 端到端 30-60s 内完成
- [ ] 输出 xlsx 在 `output/` 下, B4-B9 6 个 cell 都 ≥ 80 字且标"2026 预测"
- [ ] `streamlit run app.py` Tab 1 末尾能看到"智能体自动填写"区块, 可提交 + 下载
- [ ] 现有 `controllers.agent.run_excel_flow` 仍能跑通 (回归 OK)
- [ ] `STATUS.md` v5 增量章节已写
- [ ] 6 个 commit 都到位, commit message 规范
